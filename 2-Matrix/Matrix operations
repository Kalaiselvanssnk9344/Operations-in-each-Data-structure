MATRIX OPERATIONS

NORMAL MATRIX
- search in 2d matrix
- rotate by 90 deg
- left rotate each row of matrix K times
- print matrix in diagonal pattern
- set the row and column all values to 0 when the mat[i][j] == 0

- 


MATRIX WITH 0S AND 1s
- Find row with min 1s
- Find row with max 1s

SORTED MATRIX
- search in sorted matrix (last value of first row is less than the first value of second row)
- Search in sorted matrix II (last value of first row not to be less than first value of second row)
- Find a peak element (element is peak, if it is greather than all its adjacent elements)
